import React from "react";
import { PlayerControls, VideoPlayer } from "../../molecules";

const Player = (): JSX.Element => (
  <>
    <VideoPlayer />
    <PlayerControls />
  </>
);

export default Player;
